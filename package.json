{
  "name": "gtr-rentals-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start"
  },
  "dependencies": {
    "better-sqlite3": "12.8.0"
  }
}
